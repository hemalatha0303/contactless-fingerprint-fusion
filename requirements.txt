# Core matching pipeline (matches assignment brief section 2.1)
opencv-python-headless>=4.5   # headless build: avoids libGL.so.1 errors on
                                # free cloud containers (Streamlit Cloud, HF
                                # Spaces, Render, etc. have no display/GUI libs).
                                # Swap to opencv-python only if you need
                                # cv2.imshow for local debugging (test_pipeline.py).
numpy>=1.20
scikit-learn>=1.0
scikit-image>=0.19
scipy>=1.7
pandas>=1.3
matplotlib>=3.4

# Deployment / demo app
streamlit>=1.30
joblib>=1.3
